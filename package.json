{
  "name": "react-dose-ecosystem",
  "private": true,
  "description": "React Dose monorepo — CLI, UI design system, templates, and apps",
  "scripts": {
    "cli:dev": "pnpm --filter create-react-dose dev",
    "cli:verify": "node packages/cli/scripts/scaffold-verify.mjs",
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
